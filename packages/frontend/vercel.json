{
  "framework": "vite",
  "buildCommand": "cd ../.. && pnpm install --filter frontend... && cd packages/frontend && pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "dist"
}
